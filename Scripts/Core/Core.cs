using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 所有重要的动作
public class Core : PlatformerModel
{
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
